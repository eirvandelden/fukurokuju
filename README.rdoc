== README

This application shows quotes created by users, including backgrounds and music. Users can create new quotes, add background and audio files and make these quotes accessible for other users.
Users can define new colours to be used on the background, using hex values.

== Setup

* install gems
* create and migrate database
* seed the database
* create a user